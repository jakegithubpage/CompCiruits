#pragma once
#include <stdint.h>
#include <stdio.h>
#include <stddef.h>


typedef struct CB_Ckt CB_Ckt;

int BaseGUI_Run(CB_Ckt *ckt, unsigned nodeCount);



/* As of now Base Builder GUI's header is quite empty, but 
if needed in future, expansion of circuit builds and GUI features 
will need structs, classes, and header use*/


